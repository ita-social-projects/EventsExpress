// this constant is used to get a token from localStorage
export const constToken = 'token';

// this constant is used to get a jsonwebtoken from localStorage
export const constJsonwebtoken = 'jsonwebtoken';
