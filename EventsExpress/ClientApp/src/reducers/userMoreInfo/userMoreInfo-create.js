import initialState from "../../store/initialState";

export const reducer = (state = initialState.userMoreInfo, action) => {
    switch (action.type) {
        default:
            break;
    }
    return state;
};
